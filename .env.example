# Open Labels Initiative SDK Environment Template
# Copy to .env.local / .env and fill in as needed.

# -----------------------------------------------------------------------------
# OLI REST API (read endpoints)
# -----------------------------------------------------------------------------
# Required for protected OLI read endpoints (labels/search/analytics).
OLI_API_KEY=

# Optional browser-exposed fallback (avoid in production if you can proxy instead).
NEXT_PUBLIC_OLI_API_KEY=

# Optional API base URL override.
# SDK default: https://api.openlabelsinitiative.org
OLI_API_BASE_URL=
NEXT_PUBLIC_OLI_API_BASE_URL=

# -----------------------------------------------------------------------------
# Dynamic Wallet (only if your app uses Dynamic)
# -----------------------------------------------------------------------------
# Required by DynamicContextProvider in frontend integrations.
NEXT_PUBLIC_DYNAMIC_ENVIRONMENT_ID=

# -----------------------------------------------------------------------------
# Sponsorship / Paymaster (onchain attest write flow)
# -----------------------------------------------------------------------------
# SDK resolution order:
# 1) adapter option paymasterUrl
# 2) submit context paymasterUrl
# 3) OLI_COINBASE_PAYMASTER_URL
# 4) NEXT_PUBLIC_COINBASE_PAYMASTER_URL
# 5) SDK built-in default (⚠ a shared key — set your own for production)
#
# The SDK ships a built-in fallback paymaster URL that embeds a Coinbase CDP
# API key in the path. That key is visible in the npm bundle and on GitHub,
# so it should be treated as shared/demo-only. Set one of the variables below
# with your own Coinbase CDP paymaster URL for any production workload to
# avoid quota exhaustion from other SDK consumers.
# Get your URL at: https://portal.cdp.coinbase.com → Paymaster
OLI_COINBASE_PAYMASTER_URL=
NEXT_PUBLIC_COINBASE_PAYMASTER_URL=

# -----------------------------------------------------------------------------
# GitHub Integration (contributions module — server-side only)
# -----------------------------------------------------------------------------
# Required to call submitProjectContribution() / createGitHubPullRequestClient().
# Use a fine-grained Personal Access Token (PAT) with "Contents" (read+write)
# and "Pull requests" (read+write) permissions on the target repositories.
# NEVER expose this under a NEXT_PUBLIC_ prefix or in client-side bundles.
GITHUB_TOKEN=

# Optional: override the GitHub username / org used as the PR fork owner.
# Defaults to the token owner's account.
# GITHUB_FORK_OWNER=

# -----------------------------------------------------------------------------
# Local testing flags
# -----------------------------------------------------------------------------
# Set to 1 to run the live read API smoke test in this repository.
RUN_LIVE_TESTS=0
